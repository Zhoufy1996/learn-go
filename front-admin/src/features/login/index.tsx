/** @format */

import React from 'react';
import { DatePicker } from 'antd';

const LoginView = () => {
    return (
        <div>
            <DatePicker />
        </div>
    );
};

export default LoginView;
